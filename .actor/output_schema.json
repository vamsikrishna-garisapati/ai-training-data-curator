{
  "$schema": "https://apify-projects.github.io/actor-json-schemas/output.json?v=0.7",
  "actorOutputSchemaVersion": 1,
  "title": "AI Training Data Curator Output",
  "properties": {
    "dataset": {
      "type": "string",
      "title": "Training pages",
      "description": "Crawled pages with url, title, text, language, word count, author, and published date.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "rejected": {
      "type": "string",
      "title": "Rejected pages",
      "description": "Pages filtered out during crawling (only when Export rejected pages is enabled).",
      "template": "{{storages.datasets.rejected.apiUrl}}/items"
    },
    "summary": {
      "type": "string",
      "title": "Run summary",
      "description": "Crawl statistics: pages crawled, saved, filtered, and failed.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/%23SUMMARY"
    }
  }
}
